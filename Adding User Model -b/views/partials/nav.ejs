<nav
  class="bg-gradient-to-r from-blue-500 to-blue-700 shadow-lg shadow-blue-200/40 p-0 md:px-16 px-4 transition-all duration-300">
  <div class="flex items-center justify-between h-16">
    <div class="flex items-center gap-3">
      <span class="text-white text-2xl font-extrabold tracking-tight select-none">Air<span
          class="text-blue-200">BnB</span></span>
    </div>
    <ul class="flex gap-2 md:gap-5 font-semibold">
      <li
        class="<%= tab === 'home' ? 'bg-white/30 text-blue-800 shadow' : 'text-white' %> hover:bg-white/30 hover:text-blue-800 hover:shadow px-4 h-10 flex items-center rounded-lg transition-all duration-150">
        <a href="/">Home</a>
      </li>
      <% if(isLoggedIn) { %>
        <% if(user?.userType=="regular" ) { %>
          <li
            class="<%= tab === 'addHome' ? 'bg-white/30 text-blue-800 shadow' : 'text-white' %> hover:bg-white/30 hover:text-blue-800 hover:shadow px-4 h-10 flex items-center rounded-lg transition-all duration-150">
            <a href="/add-home">Add Home</a>
          </li>
          <li
            class="<%= tab === 'fav' ? 'bg-white/30 text-blue-800 shadow' : 'text-white' %> hover:bg-white/30 hover:text-blue-800 hover:shadow px-4 h-10 flex items-center rounded-lg transition-all duration-150">
            <a href="/fav">Favorites</a>
          </li>
          <% } %>
            <% if(user?.userType=="host" ) { %>
              <li
                class="<%= tab === 'editHome' ? 'bg-white/30 text-blue-800 shadow' : 'text-white' %> hover:bg-white/30 hover:text-blue-800 hover:shadow px-4 h-10 flex items-center rounded-lg transition-all duration-150">
                <a href="/host/editHome">Edit Home</a>
              </li>
              <% } %>

                <li
                  class="<%= tab === 'logout' ? 'bg-white/30 text-blue-800 shadow' : 'text-white' %> hover:bg-white/30 hover:text-blue-800 hover:shadow px-4 h-10 flex items-center rounded-lg transition-all duration-150">
                  <a href="/logout">Logout</a>
                </li>
                <% } %>
                  <% if(!isLoggedIn) { %>
                    <li
                      class="<%= tab === 'login' ? 'bg-white/30 text-blue-800 shadow' : 'text-white' %> hover:bg-white/30 hover:text-blue-800 hover:shadow px-4 h-10 flex items-center rounded-lg transition-all duration-150">
                      <a href="/login">Login</a>
                    </li>
                    <li
                      class="<%= tab === 'signup' ? 'bg-white/30 text-blue-800 shadow' : 'text-white' %> hover:bg-white/30 hover:text-blue-800 hover:shadow px-4 h-10 flex items-center rounded-lg transition-all duration-150">
                      <a href="/signup">SignUp</a>
                    </li>
                    <% } %>
    </ul>
  </div>
</nav>